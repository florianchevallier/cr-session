{
  "name": "cr-session",
  "version": "1.0.3",
  "private": true,
  "scripts": {
    "build": "npm run build -w backend && npm run build -w frontend",
    "validate": "npm run build"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
